export const metadata = {
  title: '批量操作',
  description: '处理大型工作区和批量依赖项更新。',
};

# 批量操作

处理大型工作区和批量依赖项更新。PCU 提供高效的批量操作功能处理大规模依赖管理。{{ className: 'lead' }}

## 批量检查

对大型工作区进行批量依赖检查：

```bash
# 并行检查所有包
pcu check --parallel

# 限制并发数
pcu check --concurrency=5
```

## 性能优化

了解如何优化 PCU 在大型项目中的性能表现。
