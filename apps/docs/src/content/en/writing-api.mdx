export const metadata = {
  title: 'Writing API Documentation',
  description:
    'Create comprehensive API documentation using Properties, Tags, and specialized components.',
};

# Writing API Documentation

Create comprehensive API documentation that developers love. Learn to use Properties for parameters, Tags for HTTP methods, and specialized components that make API references clear and actionable. {{ className: 'lead' }}

## Properties Lists

Document API parameters with `<Properties>` and `<Property>`:

<Properties>
  <Property name="id" type="string">
    Unique identifier for the resource. Automatically generated when the resource is created.
  </Property>

  <Property name="name" type="string">
    Display name of the resource. Must be between 1 and 100 characters.
  </Property>

  <Property name="email" type="string">
    Valid email address. Must be unique across all users.
  </Property>

  <Property name="created_at" type="timestamp">
    ISO 8601 timestamp indicating when the resource was created.
  </Property>
</Properties>

```mdx
<Properties>
  <Property name="id" type="string">
    Description of the parameter and its usage.
  </Property>
</Properties>
```

## HTTP Method Tags

Tags automatically get colored based on HTTP methods:

<div className="flex gap-2 flex-wrap">
  <Tag>GET</Tag>
  <Tag>POST</Tag>
  <Tag>PUT</Tag>
  <Tag>DELETE</Tag>
  <Tag color="zinc">CUSTOM</Tag>
  <Tag color="emerald">SUCCESS</Tag>
  <Tag color="rose">ERROR</Tag>
</div>

```mdx
<Tag>GET</Tag>
<Tag>POST</Tag>
<Tag color="emerald">SUCCESS</Tag>
```

## API Best Practices

- Always document all parameters
- Include example requests and responses
- Use proper HTTP status codes
- Provide clear error messages
- Include authentication requirements

## Next Steps

Complete your documentation journey with [Advanced Features](/writing-advanced).
