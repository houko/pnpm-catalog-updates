# Allow postinstall scripts for native modules
enable-pre-post-scripts=true

# Approve these packages that need build scripts in pnpm v11
# @parcel/watcher, @swc/core, esbuild, msw, sharp
public-hoist-pattern[]=*
